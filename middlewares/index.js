const validateBody = require('./validateBody');
const isValidId = require('./isValidId');
const validateContactStatus = require('./validateContactStatus');

module.exports = { validateBody, isValidId, validateContactStatus };
